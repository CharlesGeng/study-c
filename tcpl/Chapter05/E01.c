#include "C05.h"

int main()
{
    int *pi = 0;
    while (getint(pi) != EOF)
        printf("you input: %d\n", *pi);
    return 0;
}

int getint(int *pi)
{
    int c, sign;
    return c;
}
